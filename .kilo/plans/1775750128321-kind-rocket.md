# Plan: corregir error de Gradio por `allow_flagging`

## Hallazgos
- El error proviene de Gradio `6.11.0` en la `venv`, donde `gr.Interface.__init__()` ya no acepta `allow_flagging`.
- La firma actual usa `flagging_mode` en lugar de `allow_flagging`.
- Hay dos notebooks con el argumento obsoleto:
  - `002_Adquisicion_Corpus/008_App_Transcripcion_Gradio.ipynb`
  - `001_python/002_Python_para_PLN.ipynb`

## Cambios a implementar
1. Reemplazar `allow_flagging="never"` por `flagging_mode="never"` en ambos notebooks.
2. Mantener intacta la configuración restante de `gr.Interface` para no alterar el comportamiento didáctico ni la UI.
3. Revisar si el comentario asociado en `008_App_Transcripcion_Gradio.ipynb` sigue siendo correcto tras el cambio y ajustarlo solo si hace falta.

## Verificación
1. Confirmar que no queden ocurrencias de `allow_flagging` en el repositorio relevante.
2. Validar, con la `venv`, que las celdas que construyen `gr.Interface(...)` ya no fallen por argumentos inesperados.
3. Si corresponde, ejecutar una comprobación mínima de importación/instanciación con `gradio 6.11.0` para asegurar compatibilidad.

## Resultado esperado
- Los notebooks vuelven a ser compatibles con la versión actual de Gradio instalada en `.venv`.
- La creación de la interfaz deja de lanzar `TypeError: BlockContext.__init__() got an unexpected keyword argument 'allow_flagging'`.
